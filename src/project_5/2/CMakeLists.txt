SET(SOURCE_FILES main.c)

FILE(INSTALL ${CMAKE_CURRENT_SOURCE_DIR}/stdin DESTINATION ${CMAKE_CURRENT_BINARY_DIR})

ADD_EXECUTABLE(project_5_2 ${SOURCE_FILES})

TARGET_LINK_LIBRARIES(project_5_2 common)